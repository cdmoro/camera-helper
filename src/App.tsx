import React, { FC } from 'react';
import Camera from './components/Camera';

const App: FC = () => {
  return (
    <div className="App h-screen">
      <Camera />
    </div>
  );
}

export default App;
